{
  "extends": "../typescript-config/react-library.json",
  "compilerOptions": {
    "baseUrl": "src",               // 👈 important: makes @/* point inside src
    "rootDir": "src",               // 👈 ensure compiler treats src as root
    "outDir": "dist",               // 👈 compiled files go here
    "paths": {
      "@/*": ["./*"]                // 👈 @/xyz → src/xyz
    }
  },
  "include": [
    "src",                          // include all source files
    "../types/**/*.d.ts"            // shared types
  ],
  "exclude": [
    "dist",
    "build",
    "node_modules"
  ]
}
