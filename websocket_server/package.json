{
  "name": "websocket_server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node --project ./tsconfig.json ./src/server.ts",
    "check-types": "tsc --noEmit",
    "build": "pnpm --filter \"@repo/db\" build && tsc --project tsconfig.json",
    "start": "NODE_ENV=production node ./dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "@repo/db": "workspace:*",
    "@types/express": "^5.0.6",
    "@types/ws": "^8.18.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "ws": "^8.20.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/ws": "^8.18.1",
    "typescript": "^6.0.3"
  }
}
